export type DeferredState = 'PENDING' | 'FULFILLED' | 'REJECTED';
type Callback<T> = (value: T) => any;
type Errback = (reason: any) => any;
/**
 * A Promise-like class that allows you to resolve externally at a later time.
 * ```
 * const dfd = new Deferred<string>();
 * // ... do some other things ...
 * dfd.resolve('done');
 */
export declare class Deferred<T = any> implements PromiseLike<T> {
    static resolve<S = any>(value: S): Deferred<S>;
    static reject(error?: unknown): Deferred;
    static fromPromise<T = any>(promise: PromiseLike<T>): Deferred<T>;
    private _resolver;
    private _promise;
    private _state;
    constructor();
    get state(): DeferredState;
    resolve: (value: T) => void;
    reject: (reason: unknown) => void;
    then: (callback?: Callback<T>, errback?: Errback) => Promise<any>;
    catch: (errback?: ((reason: any) => any) | undefined) => Promise<any>;
    get isPending(): boolean;
    get isFulfilled(): boolean;
    get isRejected(): boolean;
}
export {};
